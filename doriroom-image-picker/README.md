# doriroom-image-picker

A Capacitor plugin for selecting multiple images from the device gallery with a selection limit.

## Install

```bash
npm install doriroom-image-picker
npx cap sync
```

## API

<docgen-index>

* [`pickImages(...)`](#pickimages)

</docgen-index>

<docgen-api>
<!--Update the source file JSDoc comments and rerun docgen to update the docs below-->

### pickImages(...)

```typescript
pickImages(options: { limit: number; }) => Promise<{ paths: string[]; }>
```

| Param         | Type                            |
| ------------- | ------------------------------- |
| **`options`** | <code>{ limit: number; }</code> |

**Returns:** <code>Promise&lt;{ paths: string[]; }&gt;</code>

--------------------

</docgen-api>
